http:
  services:
    homer:
      loadBalancer:
        servers:
          - url: "http://homer:8080"
  routers:
    homer:
      rule: "Host(`dashboard.dev.{{ velox_domain }}`)"
      entryPoints:
        - "web"
      service: "homer"
