http:
  services:
    metabase:
      loadBalancer:
        servers:
          - url: "http://metabase:3000"
  routers:
    metabase:
      rule: "Host(`metabase.dev.{{ velox_domain }}`)"
      entryPoints:
        - "web"
      service: "metabase"

