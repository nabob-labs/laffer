http:
  services:
    points-{{ network }}:
      loadBalancer:
        servers:
          - url: "http://points-bot-{{ network }}:8080"

  routers:
    points-{{ network }}-ssl:
      rule: "Host(`points-{{ network }}.{{ velox_domain }}`)"
      entryPoints:
        - "websecure"
      tls: {}
      service: "points-{{ network }}"

    points-{{ network }}:
      rule: "Host(`points-{{ network }}.{{ velox_domain }}`)"
      entryPoints:
        - "web"
      service: "points-{{ network }}"
