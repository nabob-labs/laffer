http:
  services:
    archive-{{ instance.name }}:
      loadBalancer:
        servers:
          - url: "http://archive-{{ instance.name }}:8080"

  routers:
    archive-{{ instance.name }}-ssl:
      rule: "Host(`api-archive-{{ instance.name }}-{{ hostname }}.{{ velox_domain }}`) || Host(`api-archive-{{ instance.name }}.{{ velox_domain }}`)"
      entryPoints:
        - "websecure"
      tls: {}
      service: "archive-{{ instance.name }}"

    archive-{{ instance.name }}:
      rule: "Host(`api-archive-{{ instance.name }}-{{ hostname }}.{{ velox_domain }}`) || Host(`api-archive-{{ instance.name }}.{{ velox_domain }}`)"
      entryPoints:
        - "web"
      service: "archive-{{ instance.name }}"
