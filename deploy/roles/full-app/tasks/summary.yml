- name: Display URLs
  debug:
    msg:
      - "Velox GraphQL: https://api-{{ velox_network }}-{{ hostname }}.{{ velox_domain }}/graphql"
      - "Velox GraphiQL: https://graphiql-{{ velox_network }}-{{ hostname }}.{{ velox_domain }}"
      - "Velox metrics: http://{{ tailscale_ip }}:{{ velox_metrics_port }}"
      - "Faucet bot: https://faucet-{{ velox_network }}-{{ hostname }}.{{ velox_domain }}/mint"
      - "GraphiQL: http://{{ tailscale_ip }}:{{ graphiql_port }}"
      - "Perp Liquidator: https://liquidator-{{ velox_network }}-{{ hostname }}.{{ velox_domain }}/status"
      - "CometBFT P2P: http://{{ tailscale_ip }}:{{ cometbft_p2p_port }}"
      - "CometBFT RPC: http://{{ tailscale_ip }}:{{ cometbft_rpc_port }}"
      - "CometBFT metrics: http://{{ tailscale_ip }}:{{ cometbft_metrics_port }}"
      - "deployment_name: {{ deployment_name }}"
      - "deployment_dir: {{ deployment_dir }}"
